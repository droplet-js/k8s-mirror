kind: pipeline
name: default

steps:
- name: k8s
  image: docker:dind
  volumes:
  - name: dockersock
    path: /var/run/docker.sock
  commands:
  - docker images

volumes:
- name: dockersock
  host:
    path: /var/run/docker.sock